import { View, Text } from "dripsy";

export const HeaderLeft = () => {
  return (
    <View sx={{ marginLeft: 4 }}>
      <Text sx={{ fontSize: 24, fontWeight: "bold" }}>Vetra</Text>
    </View>
  );
};
